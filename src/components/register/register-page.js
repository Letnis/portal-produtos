import React, { Component } from 'react';

import Title from '../title';

export default (props) => {
  return (
    <section className="section is-info is-bold">
      <div className="container">
        <Title text="Nova Conta" />
      </div>
    </section>
  );
}
